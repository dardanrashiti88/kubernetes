name: simple kubernetes deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v3

    - name: Set up kubectl
      uses: azure/setup-kubectl@v3

    - name: Echo message
      run: |
        echo "✅ Workflow is ready!"
        echo "When you have a Kubernetes cluster, add the KUBECONFIG_B64 secret"
        echo "and add deployment steps to deploy your applications."
